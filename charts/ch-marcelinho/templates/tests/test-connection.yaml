apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ch-marcelinho.fullname" . }}-test-connection"
  labels:
    {{- include "ch-marcelinho.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ch-marcelinho.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
