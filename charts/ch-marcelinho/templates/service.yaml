apiVersion: v1
kind: Service
metadata:
  name: {{ include "ch-marcelinho.fullname" . }}
  labels:
    {{- include "ch-marcelinho.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ch-marcelinho.selectorLabels" . | nindent 4 }}
